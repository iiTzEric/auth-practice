export default function Home() {
  return (
    <div style={{ fontFamily: 'sans-serif', padding: '2rem' }}>
      <h1>Auth Practice API</h1>
      <p>
        This is a backend-focused project. Try the API routes directly, e.g.{' '}
        <code>/api/public/info</code> (added in Stage 2), or check{' '}
        <code>/docs</code> once Swagger UI is wired up (Stage 5).
      </p>
    </div>
  );
}
